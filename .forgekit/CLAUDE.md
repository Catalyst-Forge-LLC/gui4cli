# Claude / coding-agent notes (GUI4CLI)

Same commit policy as `.forgekit/AGENTS.md`:

- Plain `git commit -m` / `-F` only — no unrequested trailers.
- Do not invent co-author or tooling attribution lines.
- Read `.forgekit/workflow_tracking.json` at session start; update it after substantive work.
- No app scaffolding until `docs/PHASE_1_BRIEF.md` is locked and the user confirms.
