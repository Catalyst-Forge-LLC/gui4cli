# Smell Check overlay (GUI4CLI)

Point: `node_modules/smellcheck/rules/core.md`. Home and README use
`landing.md`. Install, detect, window, CLI, and `--build` docs are core
only.

## Pronouns

- Site home, README, install: **you** (the person with a script).
- The product is **GUI4CLI** / **it**.
- No corporate we. "Built by Catalyst Forge LLC" stays.

## Terms that pass here

- windowd, NW.js, Vite, Commander, yargs, FormSpec, detect, `--build`,
  `--json`, last-run
- in place: wrap the original script path; do not copy the file
- Instant GUI: open a window without writing a project folder

## Protected

None beyond the Smell Check maxims.

## House extras

- Cursor's JavaScript Debug Terminal, `taskkill`, and
  `~/.gui4cli/lastrun/` belong on Install, Window, or Development. Not
  the home hero.
- Do not title a section "Honest limits." Name the limits.
