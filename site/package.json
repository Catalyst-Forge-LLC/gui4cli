{
	"name": "gui4cli-site",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"docs:build": "node scripts/build-docs.mjs",
		"dev": "pnpm docs:build && localslip claim gui4cli-site --port 5201 && filepress dev --host 0.0.0.0",
		"build": "pnpm docs:build && filepress build && node scripts/strip-blog.mjs",
		"preview": "filepress preview",
		"check": "filepress check",
		"ship": "pnpm build && wrangler pages deploy build --project-name=gui4cli",
		"deploy": "pnpm ship"
	},
	"devDependencies": {
		"getfilepress": "^0.1.39",
		"wrangler": "^4.28.0"
	},
	"pnpm": {
		"overrides": {
			"sharp": "0.35.4",
			"cookie@<0.7.0": "0.7.2"
		},
		"onlyBuiltDependencies": [
			"getfilepress"
		]
	}
}
